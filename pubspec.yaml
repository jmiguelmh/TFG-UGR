name: tfg
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # webservices
  http: ^0.13.6
  http_parser: ^4.0.2
  path_provider: ^2.0.15

  # sensors
  permission_handler: ^10.2.0
  connectivity_plus: ^4.0.1
  flutter_activity_recognition: ^2.0.0
  location: ^4.4.0
  flutter_blue_plus: ^1.5.2

  # surveys
  research_package: ^1.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
